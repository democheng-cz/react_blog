import React, { memo } from 'react';
import {BlogEditWrapper} from './style'

  const BlogEdit = memo(() => {
    return (
    <BlogEditWrapper>
      BlogEdit
    </BlogEditWrapper>
    )
    })
    export default BlogEdit